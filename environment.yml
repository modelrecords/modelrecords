name: modelrecords
channels:
  - conda-forge
  - defaults
dependencies:
  - pip:
    - pyyaml
    - semantic_version
    - jinja2
    - omegaconf
    - requests
    - pypdf
    - openai
    - pelican[markdown]
    - invoke
    - livereload
    - pytailwindcss
    - pelican-redirect
    - matplotlib
